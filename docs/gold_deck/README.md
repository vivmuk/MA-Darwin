# Gold deck (Phase 0)

A manually authored gold deck and reviewer sign-off are required before
Phase 1 content quality work proceeds into generation (PRD §12).

Placeholders:

- `gold_deck.pptx` — approved reference deck (not yet committed)
- `gold_deck_signoff.md` — reviewer name, date, and notes

Until sign-off exists, use synthetic fixtures in tests only, clearly labelled
`SYNTHETIC EXAMPLE`.
