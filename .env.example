# Copy to .env and fill in. The .env file is gitignored and read only by the
# backend (server/server.js). This key is NEVER exposed to the frontend.
ANTHROPIC_API_KEY=

# Optional: override the server port (default 3000)
# PORT=3000
