# Changelog — skill v2

## v2.0.0 (proposal — pending human approval)

Proposed from the live Darwin loop. Does not overwrite `skills/v1`.

- Human: "more graphs".
- Require an editable OOXML chart on every quantitative slide and on Backup
  when it restates numeric endpoints.
- Visual-density rule: >6 bullets on a quantitative slide also needs a
  chart/table; >3 numeric claims makes a chart mandatory.
- Deck-level floor: the 8-slide MSL deck must include at least 2 editable
  charts (evidence + one other), checked in validate.
